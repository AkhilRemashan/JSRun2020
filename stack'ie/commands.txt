CREATE PACKAGE.JSON
npm init -y

INSTALL EXPRESS, CORS(Cross Origin Resource Sharing), MONGOOSE DOTENV
npm install express cors mongoose dotenv

INSTALL NODEMON
npm install -g nodemon

#BACKEND RUN NODEMON (Windows Permission Issue)
nodemon server
[
    => npm install --save nodemon
    "scripts": {
        "server": "nodemon server.js"
    },
    => npm run server
]